type Data = {
  ceo: string;
  cto: string;
  name: string;
};

export type UseData = {
  company: Data;
};

export type Feature = {
  img: string;
  title: string;
  text: string;
  selected: boolean;
};
